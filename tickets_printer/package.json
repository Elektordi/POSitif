{
  "name": "positif_tickets_printer",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "nodemon --watch src -e js,ts,json --exec \"ts-node src/index.ts\"",
    "build": "tsup src/index.ts --format cjs --clean",
    "start": "ts-node dist/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.1",
    "tsup": "^6.7.0",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@node-escpos/core": "^0.4.0",
    "@node-escpos/usb-adapter": "^0.3.1",
    "dotenv": "^16.0.3",
    "got": "^11.8.2",
    "socket.io-client": "^4.6.1",
    "usb": "^2.9.0"
  }
}
